node_modules/
dist/
# MCP server dist is committed (pre-built for plugin users)
!mcp/registry/dist/
.env*
!.env.example
*.log
.DS_Store
Thumbs.db

# Plugin state (user-specific, not committed to plugin repo)
# Note: .sdlc/ is created in USER's project, not here
.sdlc/state.json
.sdlc/history/
.sdlc/costs/
